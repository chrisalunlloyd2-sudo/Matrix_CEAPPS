# 🧬 QWEN PEDAGOGY ROUTINE: SUBSTRATE MANIFESTATION [GEN 8]

## 🔬 OBJECTIVE
To train Qwen Code to autonomously navigate the 32-bit Android resource constraints and satisfy the security rigidness of its own internal checks.

## 🏗️ TRAINING SEQUENCE
1. **[HARVEST]** - Read the last 10 chat logs to identify "Credential Error" patterns.
2. **[STEER]** - Implement the ECJ -> D8 -> APKSIGNER pipeline as a high-speed alternative to Python-heavy build scripts.
3. **[FENCE]** - Verify the 512MB RAM physical boundary by running a heavy inference loop.
4. **[HOOK]** - Manifest the MLvoca public endpoint in `settings.json`.

## 🛠️ ORCHESTRATION MODIFICATIONS
- **Workspaces:** Always initiate tasks within a dedicated `~/workspaces/` directory to utilize optimized Git I/O.
- **Shadow Execution:** Use `shadow_executor.py` for high-risk substrate mutations.
- **Triton Chooser:** Prefer "Fast-Go-Kernel" (agy-go) for CLI commands and "Deep-Python-Kernel" for intent parsing.

## 🌡️ THERMAL MANDATE
All training loops MUST adhere to the **Cooldown Ratio 1:1**. If an inference call takes 5 seconds, the script MUST sleep for 5 seconds.
